# Literature review — staging area

All the raw literature-review / positioning documents, gathered here from across the workspace to be
**processed later** (distill → memory, feed the paper's related-work, then archive/trim). Nothing here is
canonical yet — the distilled verdicts live in the **memory `lit-*` corpus** (listed at the bottom) and the
*published* consolidation is the **`site/` research hub** (also below). This folder is the inbox between them.

## What's here

| Doc | Date | What it is | To deal with |
|---|---|---|---|
| **`STRATEGY.md`** | Jun 27 | The **7-search method-directions verdict** (flat-vs-tower, ES/QD demoted). Carries two load-bearing principles — **bias-about-WORLD vs bias-about-COGNITION** (impose structure only for true task invariants) and **emergence-as-a-measured-outcome** (don't legislate roles/phases — the red team needs a *real* emergent target) — plus a **~70-paper bibliography**. | **Extract** the two principles into a design doc (or the `emergence-framing` memory); **keep the bibliography** for `site/related-work` + the paper; **trim** the dated tactics (CDS regularizer, Go-Explore, MERL-if-stalls). |
| **`asymmetry_litreview.html`** | Jul 23 | *Symmetry-Breaking in Parameter-Shared Multi-Agent Coverage* — how identical shared-policy agents can differentiate; ties to the flood / no-emergence finding and the deploy-then-release wrapper. | Reconcile against `lit-symmetry-break-inference-verdict` memory (flood = 1-WL + Angluin); fold any delta, then archive. |
| **`positioning_portfolio.html`** | Aug 4 | *Zymera — Positioning & Paper Portfolio* — where the work sits vs prior art + the multi-paper split. Fairly current. | Feed into paper planning; reconcile with `project-multipaper-portfolio-strategy` + `lit-architecture-positioning-verdict`. |
| **`slm_marl_survey_brief.html`** | Aug 5 | *A language model for Zymera — what the evidence supports* — whether an SLM/LLM fits the program. | Decide if the LLM angle is in scope (see `lit-llm-gametheory-posg-gap`); if not, archive. |
| **`INSPIRATION_PAPERS.md`** | Aug 9 | **The design-influence bibliography** — every paper that shaped a design/architecture decision, by component, with "what we took" per entry. Living doc, not staged-for-processing. | Keep current (add a paper when it changes a decision); feeds `site/related-work` + the paper's citations. |

## Already consolidated — the published research hub (leave in place)
The polished, *output* form of all this lives on the live GitHub Pages site (`Project.Zymera/site/`), not here:
`related-work.html` · `inspirations.html` · `field-map.html`. When distilling the docs above, that hub is the
destination for anything paper-facing.

## Companion — the memory `lit-*` corpus (distilled verdicts, not files to move)
The *conclusions* of most lit-review already live as one-fact memory notes: `ref-survey-anchors`
(canonical surveys per sub-area), `lit-architecture-positioning-verdict`, `lit-coop-mission-selection-verdict`,
`lit-emergent-vs-authored-roles-verdict`, `lit-mrs-borrow-review`, `lit-connectivity-role-allocation`,
`lit-hierarchical-role-skill-gap`, `lit-symmetry-break-inference-verdict`, `lit-energy-turnover-relay-bridge`,
`lit-llm-gametheory-posg-gap`, `lit-real-missions-sparse-organize`, `lit-topopt-growth-for-swarm`,
`lit-multiL-arch-verdict`, `lit-coma-too-sparse-use-difference-rewards`. Check these before re-deriving anything.

---

*Staged 2026-08-08 (gathered from `docs/planning/` and `report/04_lit-positioning/`). Processing is deferred —
see the task list.*
